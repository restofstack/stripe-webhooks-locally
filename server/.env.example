PORT=8000

# Paste the whsec_... value printed by:
#   stripe listen --forward-to "http://localhost:8000/api/stripe-webhook-dev"
# Note: this is NOT the same as the signing secret on the Stripe dashboard
# webhook endpoint. The CLI prints its own secret for forwarded events.
STRIPE_WEBHOOK_SECRET_DEV=whsec_replace_me
